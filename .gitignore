# No XcodeGen / .xcodeproj anymore — SwiftPM + Makefile only (see
# plans/build-system.md). Keep this ignore so a stray Xcode-generated
# project never gets committed.
*.xcodeproj

# Xcode / build
DerivedData/
build/
dist/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.moved-aside

# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/
Packages/
